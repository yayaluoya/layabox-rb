module.exports = {
    /** 项目地址，可以是相对执行目录的地址也可以是一个绝对地址 */
    rootUrl: './',
    /** bin目录地址，相对于项目地址 */
    binUrl: '/bin',
    /** 复制bin目录地址，从bin目录下提取出的资源将会被放在该目录下，相对于项目地址 */
    _binUrl: '/.rb/bin',
    /** 端口 */
    port: 0,
    /** loader列表 */
    loader: [],
};